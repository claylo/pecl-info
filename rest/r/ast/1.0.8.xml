<?xml version="1.0" encoding="UTF-8" ?>
<r xmlns="http://pear.php.net/dtd/rest.release"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:xlink="http://www.w3.org/1999/xlink"
    xsi:schemaLocation="http://pear.php.net/dtd/rest.release
    http://pear.php.net/dtd/rest.release.xsd">
 <p xlink:href="/rest/p/ast">ast</p>
 <c>pecl.php.net</c>
 <v>1.0.8</v>
 <st>stable</st>
 <l>BSD-3-Clause</l>
 <m>tandre</m>
 <s>Extension exposing PHP 7 abstract syntax tree</s>
 <d>php-ast exports the AST internally used by PHP 7.
     php-ast is significantly faster than PHP-Parser, because the AST construction is implemented in C.
     However, php-ast may only parse code that is syntactically valid on the version of PHP it runs on.</d>
 <da>2020-08-05 13:59:27</da>
 <n>- Recommend using the new constant `ast\flags\PARAM_MODIFIER_*` when checking if parameters use constructor property promotion.
  The values of `ast\flags\MODIFIER_*` and `ast\flags\PARAM_VARIADIC` had some overlap in some php 7 versions.
  The new constants will have the same values as `ast\flags\MODIFIER_*` in PHP 8.0+, but different values in PHP 7
  (and these flags will never be set in php 7).
- Support PHP 8.0's named arguments.
- Support PHP 8.0's nullsafe operator (`?-&gt;`).</n>
 <f>45409</f>
 <g>https://pecl.php.net/get/ast-1.0.8</g>
 <x xlink:href="package.1.0.8.xml"/>
</r>